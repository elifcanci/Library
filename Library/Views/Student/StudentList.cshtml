@model List<Student>
@{
    ViewData["Title"] = "Student Page";
}

<h3 class="text-center">Student List Page</h3>
<table class="table table-striped">
    <thead>
        <tr>
            <th>#</th>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Gender</th>
        </tr>
    </thead>
    <tbody>
        @for (int i = 0; i < Model.Count; i++)
        {
            <tr>
                <th>@(i + 1)</th>
                <td>@Model[i].FirstName</td>
                <td>@Model[i].LastName</td>
                <th>@Model[i].Gender</th>
            </tr>
        }
    </tbody>
</table>
